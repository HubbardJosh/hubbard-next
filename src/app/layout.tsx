import Navbar from "@/components/navbar/navbar";
import { Metadata } from "next";
import "./styles.css";

export const metadata: Metadata = {
  title: {
    // fallback title to use when one isn't provided
    default: "Next.js Tutorial",
    // used to apply prefix/suffix to title, %s replaced with title of page and adds the rest of the string
    template: "%s | Tutorial",
  },
  description: "Generated by Next.js",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" style={{ height: "100%", overflow: "hidden" }}>
      <body style={{ height: "calc(100vh - 6rem - 4px)", margin: 0 }}>
        <header
          style={{
            backgroundColor: "ghostwhite",
            padding: "1rem",
            height: "3rem",
            alignItems: "center",
            display: "flex",
            justifyContent: "center",
            borderBottom: "2px solid black",
          }}
        >
          <Navbar
            routes={[
              { href: "/", label: "Home" },
              { href: "/products", label: "Products" },
              { href: "/docs", label: "Docs" },
              { href: "/order-product", label: "Order Product" },
              { href: "/about", label: "About" },
            ]}
          />
        </header>
        <div
          style={{
            padding: "1rem",
            height: "100%",
            overflowY: "auto",
          }}
        >
          {children}
        </div>
        <footer
          style={{
            backgroundColor: "ghostwhite",
            padding: "1rem",
            height: "3rem",
            borderTop: "2px solid black",
            alignSelf: "flex-end",
            clear: "both",
            position: "relative",
          }}
        >
          <p>Footer</p>
        </footer>
      </body>
    </html>
  );
}
